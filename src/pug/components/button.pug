mixin button
  -
    const { size = 'md', kind = 'secondary', as = 'button', attrs } = attributes;

    const classes = [
      'btn',
      `btn-${size}`,
      `btn-${kind}`
    ];

  #{as}(class=classes)&attributes(attrs)
    block
