mixin select
  select(class="form form-select")&attributes(attributes)
  
mixin option
  option&attributes(attributes)
    block

mixin select-field
  +field
    +label(for=attributes.id)= attributes.label
    +select&attributes(attributes)
