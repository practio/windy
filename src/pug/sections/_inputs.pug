mixin section-inputs
  +section-container('Inputs')
    p(class="mb-4 -mt-3 text-sm italic text-ink-lighter") TODO: Find a way to use different icons. Add more input variants, like one without the search hotkey etc
    

    div(class="space-y-4") 
      +inputWithSearchIcon Search
      +inputWithSearchIcon