mixin radio-input
  input(type="radio")&attributes(attributes)

mixin radio-input-field
  +field
    +radio-input&attributes(attributes)
    div
      +label(for=attributes.id)= attributes.label
      +field-description= attributes.description
